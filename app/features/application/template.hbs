<nav class="navbar navbar-inverse navbar-fixed-top">  
        <div class="container-fluid">
                <div class="navbar-header" href="#">
						{{#link-to 'index' class='navbar-brand'}}Link in Navbar to index{{/link-to}}
                </div>
                <ul class="nav navbar-nav">
					{{#if session.isAuthenticated}}
                        -# <li>{{#link-to 'new'}}Student Summary{{/link-to}}</li>
					{{/if}}
                </ul>
                <ul class="nav navbar-nav navbar-right">
					{{#unless session.isAuthenticated}}
                        <li><a href="#" {{action 'login' }}>Login</a></li>
					{{else}}
                        <li><a href="#" {{action 'logout' }}>Logout</a></li>
				        <li>
							<a href='http://github.com/{{session.currentUser.username}}' target='_blank'>
								<img src='{{session.currentUser.profileImageURL}}' height='30px' width='30px'/>
							</a>
						</li>
					{{/unless}}
                </ul>
        </div>
</nav> 
{{outlet}}
